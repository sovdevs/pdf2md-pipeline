[project]
name = "pdf-to-markdown"
version = "0.1.0"
description = "Batch PDF to Markdown pipeline for Italian urban planning documents"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiofiles>=25.1.0",
    "docling>=2.90.0",
    "fastapi>=0.136.0",
    "google-genai>=1.73.1",
    "pymupdf>=1.27.2.2",
    "python-dotenv>=1.2.2",
    "python-multipart>=0.0.26",
    "pyyaml>=6.0.3",
    "tenacity>=9.1.4",
    "uvicorn[standard]>=0.44.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "ruff>=0.15.11",
]

[project.scripts]
pdf2md = "pdf_to_markdown.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/pdf_to_markdown"]

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]
